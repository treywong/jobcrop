$('.company-profile-tab').html("<%= j render 'companies/reviews'%>");
$('.jobs-carousel').slick({
  infinite: true,
  slidesToShow: 3,
  slidesToScroll: 3
});

changeNumber()
function changeNumber(){
    var elements= document.getElementsByClassName("rating");
    for (var i=0; i<elements.length; i++) {
        var element = elements[i];
        var length = parseInt(element.innerHTML);
        var x=Array(length+1).join('<i class="fas fa-star"></i>');
        element.innerHTML=x;
    }
}
