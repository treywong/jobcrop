<h1>Edit your job</h1>
<%= form_for @job, url: {controller: "employer_jobs", action: "edit", method: "put" } do |form| %>
	<%= render partial: '/employer_jobs/form', object: form %>

	<div class="submit-field">
		<%= form.submit %>
	</div>
<% end %>