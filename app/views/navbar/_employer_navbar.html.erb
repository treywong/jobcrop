<nav class="navbar navbar-expand-lg navbar-light bg-white card p-0">
  <div class="container-fluid p-0">
    <%= link_to employer_dashboard_path, class:'text-center navbar-brand m-0 col-2' do %>
      <img src="/assets/jobcrop_logo.png" width="30" height="30" alt="">
      <span class="medium-font">For Employers</span>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <%= react_component("SearchWindowExtender", {}, class: 'col-8') %>

      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <i class="fas fa-bell nav-link" style="font-size:22px;"></i>
        </li>
        <li class="nav-item">
          <div class="dropdown">
              <%=image_tag(
                current_user.avatar_url(:thumb_navbar),
                class: 'rounded-circle nav-link',
                data: {toggle: 'dropdown'},
                aria: {haspopup: 'true', expanded: 'false'},
                style: "width:40px;height:40px;"
              )%>
            <div class="dropdown-menu dropdown-menu-right bg-light" aria-labelledby="user-settings">
              <%= link_to "My Profile", user_path(current_user.id), method: :get, class: 'dropdown-item' %>
              <%= link_to "Sign Out", sign_out_path, method: :delete, class: 'dropdown-item' %>
            </div>
          </div>
        </li>
        <li class="nav-item">
          <%=link_to "Dashboard", employer_dashboard_path, class:'nav-link'%>
        </li>
        <li class="nav-item">
          <a class="btn btn-outline-secondary" href="#">Post Jobs for free</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
